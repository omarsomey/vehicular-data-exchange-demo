import { React } from 'react';
import { Link } from 'react-router';

export const OverviewPage = () => {
    return (
        <div className="OverviewPage">
            <h1> Vehicular data exchange with PRE</h1>
        </div>
    );
}
